# 新能源物流车队协同调度仿真系统项目报告

## 摘要

本项目面向“中央仓库 + 新能源物流车队 + 城市动态任务”的调度场景，设计并实现了一个新能源物流车队协同调度仿真系统。系统使用无向加权图表示城市道路网络，通过 Dijkstra 最短路算法完成寻路；使用离散时间仿真模拟任务动态释放、车辆派单、行驶、充电与回仓过程；显式考虑车辆载重、电池容量、单位里程能耗、任务时间窗、充电站端口排队和多车协同等现实约束。

在调度算法方面，项目实现了最近任务优先、最大任务优先、紧急度-距离综合、拍卖式多智能体、模拟退火、大邻域搜索、Q 值启发式和 UCB 超启发式共 8 种在线动态策略；同时建立静态全信息数学规划模型，并使用 CPLEX 生成 `small / medium / large` 三种规模下的优化基线。系统还提供 Web 仪表盘，实现仿真运行、车辆路线回放、策略排名、基准结果展示和天气统计展示。

实验结果表明：动态策略在三种规模下均能完成全部任务，说明系统在电量、载重和充电约束下具有稳定可行性；CPLEX 静态全信息基线在三个规模下均取得最高综合得分，体现了全信息优化基准相对于在线实时策略的优势。

---

## 1. 项目背景与目标

### 1.1 背景

新能源物流车在城市配送中具有低排放、低噪声和运行成本较低等优势，但其调度过程比传统燃油车更复杂。主要原因包括：

- 车辆电池容量有限，不能只考虑距离，还要考虑剩余电量和回仓需求。
- 车辆载重有限，部分大重量任务可能需要多车协同完成。
- 充电站端口有限，车辆到达后可能需要排队等待。
- 城市任务是动态出现的，调度系统无法提前知道所有未来任务。
- 交通拥堵、天气等因素会影响行驶耗时和任务完成质量。

因此，本项目围绕大作业要求，构建一个具有现实约束的新能源物流车队调度仿真系统，用于比较不同在线调度策略的效果，并通过静态全信息 CPLEX 基线分析实时策略与全局优化基准之间的差距。

### 1.2 项目目标

项目目标包括：

1. 使用图结构实现城市道路网络与最短路径搜索。
2. 随机生成车辆、任务、充电站和不同规模场景。
3. 模拟任务在一段时间内动态释放。
4. 建模车辆载重、电量、充电站排队和任务时间窗约束。
5. 实现至少两种调度策略，并进一步实现多种进阶策略。
6. 支持三种以上规模的实验对比。
7. 使用 CPLEX 建立静态全信息优化基线并与动态策略对比；若求解器证明最优，则该基线可作为全局最优解，否则作为限时高质量优化基线。
8. 提供图形化界面展示调度过程和实验结果。

---

## 2. 问题定义

### 2.1 道路网络

城市道路网络被建模为无向加权图：

```text
G = (V, E)
```

其中：

- `V` 表示城市中的节点，包括仓库、任务点、充电站和道路节点。
- `E` 表示道路连接。
- 边权表示两节点之间的距离。

车辆在执行任务时，需要反复求解以下路径：

- 车辆当前位置到任务点。
- 任务点回仓库。
- 车辆当前位置到充电站。
- 充电站到任务点。
- 充电站回仓库。

这些路径均通过 Dijkstra 最短路算法计算。

### 2.2 车辆集合

设车辆集合为：

```text
K = {1, 2, ..., m}
```

每辆车具有以下属性：

| 属性 | 含义 |
|---|---|
| `capacity` | 最大载重 |
| `battery_capacity` | 电池容量上限 |
| `battery` | 当前电量 |
| `speed` | 行驶速度 |
| `energy_per_distance` | 单位距离能耗 |
| `current_node` | 当前所在节点 |
| `available_time` | 下一次可用时间 |

车辆数量有限，且车辆完成任务后需要回到中央仓库。

### 2.3 任务集合

任务集合随时间动态产生。每个任务包含：

| 属性 | 含义 |
|---|---|
| `release_time` | 任务产生时间 |
| `node_id` | 任务所在节点 |
| `x, y` | 任务坐标 |
| `weight` | 货物重量 |
| `deadline` | 截止时间 |

仿真开始时，所有任务已经在数据中生成，但在线动态策略只能在任务到达 `release_time` 后看到该任务。

### 2.4 充电站集合

充电站集合为：

```text
S = {1, 2, ..., q}
```

每个充电站包含：

| 属性 | 含义 |
|---|---|
| `node_id` | 所在节点 |
| `charge_rate` | 充电速率 |
| `ports` | 充电端口数量 |
| `_port_available_times` | 每个端口的最早可用时间 |

车辆到达充电站后，如果所有端口被占用，则需要排队等待。项目用每个端口的“最早可用时间”模拟充电站排队过程。

---

## 3. 系统设计与数据结构

### 3.1 总体架构

系统由五个主要部分组成：

| 模块 | 文件 | 作用 |
|---|---|---|
| 图结构与最短路 | `simulator/graph.py` | 加权图、Dijkstra 最短路径 |
| 数据模型 | `simulator/models.py` | 任务、车辆、充电站、事件、结果 |
| 仿真引擎 | `simulator/simulation.py` | 场景生成、动态时间推进、派单执行 |
| 调度策略 | `simulator/strategies.py` | 8 种在线动态调度策略 |
| 精确基线 | `simulator/exact_solver.py` | CPLEX 静态全信息 MIP 模型 |
| 可视化 | `dashboard.py`、`simulator/gui.py`、`simulator/web/` | Web 仪表盘与回放 |

### 3.2 图结构

`WeightedGraph` 使用邻接表存储图：

```text
node_id -> [(neighbor_id, distance), ...]
```

优点：

- 适合稀疏道路网络。
- 遍历邻接节点效率高。
- 便于动态生成随机图和地图图。

最短路径使用 Dijkstra 算法，采用 `heapq` 最小堆维护候选节点。单次最短路复杂度约为：

```text
O((V + E) log V)
```

系统还对最短路查询结果做缓存，减少仿真过程中重复计算。

### 3.3 离散时间仿真

仿真主循环位于 `FleetSimulator.run()`，流程如下：

1. 初始化任务指针、待派任务池、车辆状态和事件列表。
2. 从 `now = 0` 开始按时间步推进。
3. 将 `release_time <= now` 的任务加入待派池。
4. 找出当前空闲车辆。
5. 检查空闲车辆是否需要主动补电。
6. 调用调度策略选择任务和车辆组合。
7. 规划车辆路径和充电方案。
8. 更新车辆电量、位置、可用时间和充电站端口占用。
9. 记录任务结果与回放事件。
10. 所有任务释放完毕且车辆空闲后结束仿真。

### 3.4 充电站排队模型

每个充电站维护一个数组：

```text
_port_available_times = [t1, t2, ..., tp]
```

其中 `p` 为端口数量，`ti` 表示第 `i` 个端口最早空闲时间。车辆到达时：

```text
start_time = max(arrival_time, min(_port_available_times))
finish_time = start_time + charge_duration
```

然后更新对应端口的可用时间。这样可以模拟多端口排队和充电资源竞争。

### 3.5 多车协同模型

当任务重量超过单车载重时，如果开启 `--allow-collaboration`，系统允许多车协同完成同一任务。协同车辆组的总载重需要覆盖任务重量，任务重量按车辆容量比例分摊：

```text
carried_weight_i = task_weight * capacity_i / sum(capacity_group)
```

每辆车仍独立计算路线、电量消耗、充电等待和完成时间。任务最终完成时间取协同车辆完成时间的最大值。

---

## 4. 调度策略设计

项目实现了 8 种动态策略，均继承统一的 `DispatchStrategy` 接口。策略输入包括待派任务、空闲车辆、车辆状态、道路图、协同开关和当前时间；输出为一个任务和一组车辆。

| 策略 | 类型 | 主要思想 |
|---|---|---|
| `nearest_task_first` | 贪心启发式 | 选择距离最近的任务-车辆组合 |
| `max_task_first` | 规则启发式 | 偏向重量较大、收益较高、较紧急的任务 |
| `urgency_distance` | 综合启发式 | 综合任务紧急度、路径距离、载重和电量 |
| `auction_multi_agent` | 多智能体 | 车辆对任务出价，选择收益最高组合 |
| `metaheuristic_sa` | 元启发式 | 用模拟退火在候选决策中搜索较优解 |
| `lns_aaai_2025` | 大邻域搜索 | 对候选派单进行破坏-修复和局部重优化 |
| `reinforcement_q` | 强化学习雏形 | 用 Q 表学习不同状态下的派单偏好 |
| `hyper_heuristic_ucb` | 超启发式 | 用 UCB 在多个子规则之间动态选择 |

这些策略覆盖了大作业要求中的基础策略和进阶算法方向。

---

## 5. 评分函数与评价指标

### 5.1 单任务得分

动态仿真中，任务完成后的得分为：

```text
score = 260
        - 0.95 * response_time
        - 0.52 * distance
        - 0.25 * charging_wait
```

若任务超时，则额外扣除：

```text
overtime_penalty = 70
```

若任务最终未服务，则在总分中扣除：

```text
unserved_penalty = 60
```

该评分函数体现了题目要求：完成越早、路径越短、充电等待越少，收益越高；超时和未完成会降低总收益。

### 5.2 评价指标

| 指标 | 含义 | 趋势 |
|---|---|---|
| `completed` | 完成任务数 | 越高越好 |
| `unserved` | 未完成任务数 | 越低越好 |
| `overtime` | 超时任务数 | 越低越好 |
| `distance` | 总行驶距离 | 越低越好 |
| `avg_response_time` | 平均响应时间 | 越低越好 |
| `charging_wait` | 充电等待时间 | 越低越好 |
| `score` | 综合收益 | 越高越好 |
| `charging_sessions` | 充电会话数 | 用于观察补能频率 |
| `max_charging_wait` | 最大充电等待 | 越低越好 |
| `station_avg_utilization` | 充电站平均负荷 | 用于观察资源压力 |
| `station_peak_utilization` | 充电站峰值负荷 | 用于观察拥塞峰值 |
| `collaborative_tasks` | 协同完成任务数 | 用于展示多车协同 |

---

## 6. 静态全信息 CPLEX 基线

为了完成进阶要求，项目建立了静态全信息数学规划模型。动态策略只能看到已经释放的任务，而 CPLEX 基线假设某个时间段内的任务全部已知，相当于“上帝视角”。当 CPLEX 状态显示已证明最优时，可以称为全局最优；当状态为 `time limit exceeded` 等未证明最优时，本文将其表述为“静态全信息限时优化基线”。

### 6.1 建模思路

模型核心变量包括：

- `x[i,k]`：任务 `i` 是否由车辆 `k` 执行。
- `y[i]`：任务 `i` 是否被服务。
- `s[i]`：任务 `i` 的开始时间。
- `c[i]`：任务 `i` 的完成时间。
- `late[i]`：任务 `i` 是否超时。
- 顺序变量：约束同一车辆上多个任务的执行顺序。

目标函数综合考虑：

- 完成任务收益。
- 响应时间成本。
- 路径距离成本。
- 充电等待近似成本。
- 超时惩罚。

### 6.2 对比意义

CPLEX 静态全信息基线用于回答：

> 如果所有任务一开始就已知，系统能获得怎样的整体调度效果？

然后将该结果与动态在线策略对比，可以分析实时调度策略与全局优化基准之间的差距。

---

## 7. 实验设置

### 7.1 场景规模

项目设置三种规模：

| 规模 | 节点数 | 额外边数 | 车辆数 | 充电站数 | 任务数 | 仿真时长 |
|---|---:|---:|---:|---:|---:|---:|
| `small` | 24 | 34 | 6 | 3 | 14 | 360 |
| `medium` | 58 | 120 | 15 | 8 | 110 | 760 |
| `large` | 100 | 260 | 28 | 12 | 220 | 1250 |

### 7.2 实验命令

动态策略基准实验：

```bash
python main.py --no-oracle --allow-collaboration --seed-runs 3 --output results/summary_dynamic.json
```

充电压力场景：

```bash
python main.py --no-oracle --scales stress --strategies nearest_task_first urgency_distance lns_aaai_2025 hyper_heuristic_ucb --allow-collaboration --export-events --output results/summary_stress.json --events-output results/events_stress.json
```

动态策略与 CPLEX 静态全信息基线对比：

```bash
python main.py --allow-collaboration --exact-backend cplex --exact-scales small medium large --output results/summary_cplex.json
```

Web 可视化仪表盘：

```bash
python dashboard.py
```

### 7.3 实验数据来源

本报告主要采用 `results/summary_cplex.json` 作为同场景对比数据源，并结合 `results/summary_lns_smoke.json` 与 `results/weather_stats.json` 展示新增大邻域搜索策略。当前代码注册表中包含 8 种动态策略；若重新运行 `main.py`，`results/summary_dynamic.json` 会按当前策略列表输出完整动态策略集合。已有 `weather_stats.json` 中已经补齐 `lns_aaai_2025` 在晴朗、雨天和拥堵条件下的统计结果。

---

## 8. 实验结果与分析

### 8.1 Small 场景

| 排名 | 策略 | 模式 | 完成 | 未完成 | 超时 | 总距离 | 平均响应 | 充电等待 | 得分 |
|---:|---|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | `static_exact_fullinfo` | CPLEX 静态全信息 | 14 | 0 | 0 | 1453.18 | 54.09 | 0.00 | 2164.97 |
| 2 | `metaheuristic_sa` | 动态策略 | 14 | 0 | 1 | 1554.64 | 67.93 | 0.00 | 1858.07 |
| 3 | `nearest_task_first` | 动态策略 | 14 | 0 | 1 | 1514.98 | 69.53 | 19.77 | 1852.47 |
| 4 | `hyper_heuristic_ucb` | 动态策略 | 14 | 0 | 1 | 1523.22 | 69.65 | 0.00 | 1851.54 |
| 5 | `reinforcement_q` | 动态策略 | 14 | 0 | 1 | 1518.67 | 69.84 | 0.00 | 1851.39 |
| 6 | `lns_aaai_2025` | 动态策略 | 14 | 0 | 1 | 1514.98 | 70.10 | 0.00 | 1849.88 |

分析：小规模场景下，所有策略均完成全部任务。CPLEX 基线得分最高，且无超时任务。动态策略中，模拟退火策略得分最高；大邻域搜索也保持了完整完成率，说明候选派单的局部重优化在小规模场景下可作为稳定补充策略。

### 8.2 Medium 场景

| 排名 | 策略 | 模式 | 完成 | 未完成 | 超时 | 总距离 | 平均响应 | 充电等待 | 得分 |
|---:|---|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | `static_exact_fullinfo` | CPLEX 静态全信息 | 110 | 0 | 2 | 7773.40 | 48.12 | 0.00 | 19389.73 |
| 2 | `lns_aaai_2025` | 动态策略 | 110 | 0 | 4 | 7459.84 | 50.38 | 0.36 | 19175.62 |
| 3 | `nearest_task_first` | 动态策略 | 110 | 0 | 7 | 7402.67 | 53.28 | 0.00 | 18692.42 |
| 4 | `auction_multi_agent` | 动态策略 | 110 | 0 | 7 | 7419.32 | 54.54 | 6.08 | 18550.88 |
| 5 | `metaheuristic_sa` | 动态策略 | 110 | 0 | 7 | 7400.71 | 54.76 | 9.43 | 18536.81 |

分析：中规模场景中，任务数量增加到 110，调度压力明显上升。所有策略仍完成全部任务，但超时数量和响应时间开始拉开差距。CPLEX 基线平均响应时间最低，超时任务最少。动态策略中，大邻域搜索通过候选解破坏-修复降低了平均响应时间和超时数，表现优于单纯贪心策略。

### 8.3 Large 场景

| 排名 | 策略 | 模式 | 完成 | 未完成 | 超时 | 总距离 | 平均响应 | 充电等待 | 得分 |
|---:|---|---|---:|---:|---:|---:|---:|---:|---:|
| 1 | `static_exact_fullinfo` | CPLEX 静态全信息 | 220 | 0 | 0 | 19476.84 | 50.67 | 0.00 | 36482.61 |
| 2 | `lns_aaai_2025` | 动态策略 | 220 | 0 | 6 | 18777.53 | 51.06 | 0.00 | 36344.52 |
| 3 | `reinforcement_q` | 动态策略 | 220 | 0 | 6 | 18472.35 | 52.26 | 5.19 | 36250.64 |
| 4 | `metaheuristic_sa` | 动态策略 | 220 | 0 | 7 | 18495.16 | 52.37 | 0.00 | 36147.90 |
| 5 | `urgency_distance` | 动态策略 | 220 | 0 | 7 | 18521.30 | 52.53 | 0.00 | 36100.85 |

分析：大规模场景中，任务数量达到 220，所有策略依然完成全部任务，说明车辆规模、协同机制和充电规划能够支撑高负载场景。CPLEX 基线得分最高且没有超时任务。动态策略中，大邻域搜索和 Q-learning 都表现较强，其中大邻域搜索通过局部重优化进一步降低了响应时间。

### 8.4 最优动态策略与 CPLEX 基线对比

| 规模 | 最优动态策略 | 最优动态得分 | CPLEX 得分 | 分差 | 动态超时 | CPLEX 超时 |
|---|---|---:|---:|---:|---:|---:|
| `small` | `metaheuristic_sa` | 1858.07 | 2164.97 | 306.91 | 1 | 0 |
| `medium` | `lns_aaai_2025` | 19175.62 | 19389.73 | 214.11 | 4 | 2 |
| `large` | `lns_aaai_2025` | 36344.52 | 36482.61 | 138.10 | 6 | 0 |

总体来看，CPLEX 静态全信息基线在三种规模下均取得最高综合得分。动态策略虽然无法提前知道未来任务，但仍能保持全部任务完成，说明在线调度策略具有较强实用性。

---

## 9. 图形界面与结果展示

项目提供 Web 仪表盘作为图形化展示界面。启动命令：

```bash
python dashboard.py
```

默认地址：

```text
http://127.0.0.1:8765
```

界面功能包括：

- 选择场景规模、调度策略、随机种子、天气/路况模式。
- 运行单次仿真并播放车辆路线。
- 展示任务状态、车辆状态、充电行为和调度日志。
- 展示策略排名对比。
- 加载 `results/summary_cplex.json`、`results/summary_dynamic.json` 等结果文件。
- 展示天气统计文件 `results/weather_stats.json`。
- 支持随机图、番禺本地底图和高德地图相关模式。

图形界面使项目不仅能输出数值表格，还能直观展示车辆如何移动、如何充电、如何完成任务，增强了结果展示的可解释性。

---

## 10. 大作业要求完成情况

| 大作业要求 | 完成情况 |
|---|---|
| 使用图结构实现道路和寻路 | 已完成，邻接表 + Dijkstra |
| 有限车辆、电量上限、载重上限 | 已完成，车辆数据类显式建模 |
| 动态任务生成 | 已完成，任务按 `release_time` 释放 |
| 任务包含时间、坐标、重量 | 已完成，随机生成并可复现 |
| 完成越早、路越短收益越高 | 已完成，评分函数体现时间和距离成本 |
| 超时和未完成扣分 | 已完成，分别统计并扣分 |
| 电量不足时补能 | 已完成，自动选择充电方案 |
| 充电站排队与负荷 | 已完成，端口最早可用时间模拟排队 |
| 多车协同 | 已完成，可通过参数开启 |
| 至少两种策略 | 超额完成，共 8 种策略 |
| 至少三种规模 | 已完成，small / medium / large |
| 进阶算法 | 已完成，RL、模拟退火、大邻域搜索、UCB、多智能体 |
| CPLEX 静态全信息基线 | 已完成，三种规模均有对比结果 |
| 图形界面展示 | 已完成，Web Dashboard 支持回放和结果展示 |

---

## 11. 小组分工

可根据实际情况填写：

| 成员 | 主要工作 |
|---|---|
| 成员 A | 图结构建模、Dijkstra 最短路、随机场景生成 |
| 成员 B | 动态仿真主循环、车辆电量与充电站排队约束 |
| 成员 C | 调度策略实现、实验结果统计与分析 |
| 成员 D | CPLEX 静态基线、Web 可视化、展示视频与报告整理 |

如果小组实际人数为三人，可将 CPLEX 与可视化工作合并到成员 C 或成员 B 中。

---

## 12. 总结与展望

本项目完整实现了新能源物流车队协同调度仿真系统，满足并扩展了大作业要求。系统以图结构和最短路算法为基础，结合离散时间仿真、车辆电量约束、载重约束、充电站排队、多车协同和多策略调度，构建了一个较完整的城市物流调度实验平台。

实验结果表明：

- CPLEX 静态全信息基线在三种规模下均取得最高得分，体现了全局优化基准的价值。
- 动态策略在不知道未来任务的情况下仍能完成全部任务，说明在线启发式方法具有较强实用性。
- 不同规模下最优动态策略不同，说明调度策略需要根据场景规模和任务密度进行选择。
- Web 仪表盘显著提升了实验结果的可解释性和展示效果。

后续可以进一步扩展：

- 引入更真实的订单收益、服务时间和多仓库设置。
- 对充电策略、派单策略和大邻域搜索的破坏/修复算子进行联合优化。
- 增加更多城市真实路网数据。
- 将 Q-learning 策略扩展为更完整的强化学习训练框架。
- 增加自动化测试和更系统的实验管理。

