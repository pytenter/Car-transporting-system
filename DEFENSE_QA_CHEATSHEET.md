# 答辩问答速查表

这份文档用于答辩临场快速复习。建议答辩前每个人至少把“核心流程”“必问问题”“可视化展示顺序”三部分讲顺。

## 一、30 秒总述

本项目解决的是中央仓库新能源物流车队的动态调度问题。我们把城市道路抽象成加权图，用 Dijkstra 做路径规划；任务按时间动态释放，车辆受到载重、电量、回仓和充电站排队约束；系统实现 7 种在线调度策略，并在 `small / medium / large / stress` 多种规模下比较完成率、超时、距离、充电等待和综合得分。同时提供 Web Dashboard 展示车辆路线回放、策略排名和实验结果，并使用 CPLEX 建立静态全信息优化基线作为对照。

## 二、算法流程怎么讲

### 1. 场景生成

对应代码：`simulator/simulation.py -> build_scenario()`

讲法：

1. 先生成城市道路图，节点代表仓库、任务点、充电站或道路节点。
2. 随机生成车辆，每辆车有载重上限、电池容量、当前电量、速度和单位距离能耗。
3. 随机生成任务，每个任务有释放时间、位置坐标、货物重量和截止时间。
4. 生成充电站，每个充电站有充电速率和端口数。
5. `small / medium / large` 用于满足三种规模要求，`stress` 用于展示低电量和充电排队压力。

### 2. 路径规划

对应代码：`simulator/graph.py -> WeightedGraph.shortest_path()`

讲法：

车辆每次派单前，需要计算：

- 当前位置到任务点的最短路径；
- 任务点回仓库的最短路径；
- 如果电量不足，还要计算当前位置到充电站、充电站到任务点、任务点回仓的路径。

项目用 Dijkstra 算法求最短路，并缓存已经计算过的节点对，减少重复计算。

### 3. 动态调度

对应代码：`simulator/simulation.py -> FleetSimulator.run()`

讲法：

仿真按时间推进。每个时刻先把已经到达释放时间的任务加入待调度池，然后找出空闲车辆，再让当前策略从“待任务 + 空闲车辆”中选择派单方案。车辆执行任务后会更新位置、电量和下一次可用时间。

### 4. 电量与充电

对应代码：

- `FleetSimulator._plan_vehicle_mission()`
- `FleetSimulator._pick_charging_plan()`
- `ChargingStation.reserve()`

讲法：

系统先判断车辆当前电量是否足够完成“到任务点 + 服务 + 回仓”，并保留安全电量。如果不够，就枚举候选充电站，综合到站距离、等待时间、充电时间和负荷压力，选择最优补能方案。充电站不是无限资源，每个端口都有最早可用时间，因此会自然产生排队。

### 5. 收益评分

对应代码：`FleetSimulator._score_task()`

讲法：

评分由四部分构成：

- 完成越早，响应时间越短，分数越高；
- 路径越短，分数越高；
- 充电等待越少，分数越高；
- 超过截止时间会额外扣分；
- 未完成任务会在总分里扣分。

### 6. 多车协同

对应代码：

- `simulator/strategies.py -> _nearest_collab_group()`
- `simulator/strategies.py -> _capacity_first_collab_group()`

讲法：

如果任务重量超过单车载重，但开启 `--allow-collaboration` 后，多辆车总载重可以覆盖该任务，系统就允许多车协同完成。任务重量按车辆容量比例分摊，并在事件和可视化里记录每辆车承担的重量和路线。

## 三、老师可能问什么

### Q1：你们为什么要用图结构？

A：题目明确要求使用图结构实现道路和寻路。我们用加权图表示道路网络，节点是仓库、任务点、充电站或道路节点，边权表示路程。这样车辆调度不是简单欧氏距离，而是在图上做最短路径规划，更符合道路网络场景。

### Q2：Dijkstra 在哪里体现？

A：在 `simulator/graph.py` 的 `WeightedGraph.shortest_path()`。它使用 `heapq` 最小堆，每次取当前距离最短的节点扩展邻居，最终得到两点之间最短距离和路径节点序列。

### Q3：任务是动态的吗？

A：是。每个任务都有 `release_time`。仿真每个时刻只把当前已经释放的任务加入 pending 池，动态策略只能看到当前已释放任务，看不到未来任务。

### Q4：怎么体现新能源车辆特性？

A：车辆有电池容量、当前电量、单位距离能耗、安全电量要求；执行任务会消耗电量；电量不足时必须去充电站或仓库补能；充电还会受端口数量和排队影响。

### Q5：充电站排队怎么实现？

A：每个充电站维护一组端口可用时间 `_port_available_times`。车辆到站时选择最早可用端口，如果端口还没空出来，就要等待。`reserve()` 会更新端口被占用到的时间。现在 summary 里也输出 `charging_sessions`、`max_charging_wait`、`station_avg_utilization`、`station_peak_utilization`，用于展示负荷压力。

### Q6：为什么新增 stress 场景？

A：普通场景主要用于比较策略综合表现，`stress` 场景专门用于展示新能源车辆的关键难点：低初始电量、充电站少、端口少、充电慢、任务集中。这样老师能直观看到电量不足、充电排队和资源瓶颈对调度结果的影响。

### Q7：实现了哪些策略？

A：共 7 种：

| 策略 | 说明 |
|---|---|
| `nearest_task_first` | 最近任务优先 |
| `max_task_first` | 大重量/高价值任务优先 |
| `urgency_distance` | 同时考虑截止时间紧急度和距离 |
| `auction_multi_agent` | 多智能体拍卖式派单 |
| `metaheuristic_sa` | 模拟退火搜索候选派单 |
| `reinforcement_q` | Q 值启发式，在线更新动作偏好 |
| `hyper_heuristic_ucb` | UCB 超启发式，在多个子规则间选择 |

### Q8：强化学习是不是完整训练模型？

A：不是完整深度强化学习训练框架，而是轻量级 Q 值启发式。它把系统状态离散化，在若干派单动作间更新偏好。答辩时建议说“尝试了强化学习思想”或“Q 值启发式”，不要夸大成深度强化学习。

### Q9：CPLEX 是全局最优吗？

A：要看求解状态。如果 CPLEX 显示证明最优，就可以称为全局最优；如果状态是 `time limit exceeded`，说明在时间限制内找到可行优化解但没有证明全局最优，应称为“静态全信息限时优化基线”。这仍然有价值，因为它比动态策略拥有更强信息条件，可以作为对照。

### Q10：动态策略和 CPLEX 为什么结果不同？

A：动态策略只知道当前已释放任务，是在线实时决策；CPLEX 假设所有任务提前已知，是静态全信息模型。两者信息条件不同，所以 CPLEX 通常能更好地安排整体顺序。差距可以反映实时策略和全信息优化基线之间的距离。

### Q11：可视化界面应该展示什么？

A：建议展示三件事：

1. 单次仿真回放：看车辆路线、任务点、充电行为。
2. 策略排名：看不同策略的完成数、超时、距离、得分。
3. 压力场景或历史结果：看充电等待、充电次数、负荷压力和协同任务。

### Q12：项目相比基础要求多做了什么？

A：基础要求只需要两种策略和三种规模。我们实现了 7 种策略、4 种规模、充电站排队负荷统计、多车协同、天气/拥堵扰动、CPLEX 静态优化基线和 Web Dashboard。

## 四、可视化展示顺序

### 第一步：启动 Dashboard

```bash
python dashboard.py
```

打开：

```text
http://127.0.0.1:8765
```

讲法：

> 这是我们的可视化仪表盘，可以运行单次仿真、展示车辆回放、比较策略结果。

### 第二步：跑普通 small 场景

界面选择：

- 规模：`small`
- 策略：`urgency_distance` 或 `nearest_task_first`
- 勾选多车协同

讲法：

> 先用小规模展示完整调度流程，车辆从仓库出发，按策略选择任务，完成后回仓或补能。

### 第三步：展示策略排名

讲法：

> 这里比较的是同一场景下多种策略的完成任务数、超时任务数、总距离、充电等待和综合得分。

### 第四步：展示 stress 压力场景

先生成数据：

```bash
python main.py --no-oracle --scales stress --strategies nearest_task_first urgency_distance hyper_heuristic_ucb --allow-collaboration --export-events --output results/summary_stress.json --events-output results/events_stress.json
```

讲法：

> stress 场景专门用于展示电量和充电站约束。可以看到充电次数、最大等待时间和站点峰值利用率明显上升，说明系统确实考虑了充电站排队与负荷压力。

### 第五步：展示 CPLEX 对比

讲法：

> 这里是动态在线策略和静态全信息优化基线的对比。动态策略更贴近真实实时调度，CPLEX 基线用于评估动态策略距离更强信息条件下的优化方案还有多少差距。

## 五、最容易失分的说法

不要说：

- “CPLEX 一定求出了全局最优。”
- “reinforcement_q 是完整强化学习模型。”
- “充电站利用率只看结束时刻。”

建议说：

- “CPLEX 若证明最优就是全局最优，否则是静态全信息限时优化基线。”
- “reinforcement_q 是 Q 值启发式，体现了强化学习思想。”
- “系统输出全过程平均利用率、峰值利用率、充电会话数和最大等待时间。”

## 六、每个成员都应该会讲的最短版本

> 我们的系统用图表示道路，用 Dijkstra 求路径；任务按时间动态释放，车辆有载重和电量约束。策略层实现了最近任务、大重量任务、紧急度距离、拍卖式、模拟退火、Q 值启发式和 UCB 超启发式。仿真过程中，如果车辆电量不足，会选择充电站补能，并通过端口可用时间模拟排队。结果用完成数、超时、距离、充电等待和综合得分评价。项目还支持多车协同、stress 压力场景、CPLEX 静态全信息优化基线和 Web 可视化回放。
