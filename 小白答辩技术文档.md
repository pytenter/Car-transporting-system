# 新能源物流车队协同调度大作业答辩说明

本文档专门对应 `2026-大作业要求.txt`，用于说明本项目是如何逐项完成大作业要求的。答辩时不需要背代码，重点讲清楚：问题如何建模、用了哪些数据结构、仿真如何运行、策略如何比较、结果如何展示。

---

## 一、项目整体对应的大作业题目

大作业题目要求我们模拟一个“中央仓库 + 新能源物流车队 + 城市动态任务”的调度系统。项目实现的核心目标是：

- 用图结构表示城市道路网络。
- 随机生成车辆、任务、充电站和不同规模场景。
- 在电量、载重、时间窗、充电站排队等约束下完成车辆派单。
- 比较多种调度策略的表现。
- 用 CPLEX 建立静态全信息优化基线，与动态调度结果进行对比；如果求解器证明最优，则可称为全局最优，否则按限时优化基线解释。
- 通过 Web 仪表盘进行路线回放和结果展示。

一句话概括：本项目把大作业要求扩展成了一个完整的新能源物流调度仿真平台，从数据结构、调度算法、实验结果到可视化展示形成闭环。

---

## 二、逐项对照大作业要求

### 要求 0：使用图结构实现道路和寻路

项目完成方式：

- `simulator/graph.py` 中定义了 `WeightedGraph`。
- 道路网络用邻接表保存：每个节点保存相邻节点和边权。
- 最短路径使用 Dijkstra 算法，并用 `heapq` 最小堆加速。
- 最短路结果带缓存，避免重复计算同一对节点路径。

答辩说法：

> 我们把城市路网抽象成无向加权图，节点表示仓库、任务点、充电站或道路节点，边权表示路程。车辆每次执行任务前，都通过 Dijkstra 计算从当前位置到任务点、任务点回仓、或中途到充电站的最短路径。

对应文件：

| 文件 | 作用 |
|---|---|
| `simulator/graph.py` | 图结构、节点、边、Dijkstra 最短路 |
| `simulator/simulation.py` | 在仿真中调用最短路进行路径规划 |

---

### 要求 1：车辆数量有限，每辆车有电量上限和载重上限，模拟一段时间内动态出现的任务

项目完成方式：

- `Vehicle` 数据类中包含：
  - `capacity`：载重上限
  - `battery_capacity`：电池容量上限
  - `battery`：当前电量
  - `speed`：速度
  - `energy_per_distance`：单位距离能耗
  - `available_time`：车辆下一次可用时间
- `SCENARIO_SCALES` 定义了三种规模：
  - `small`：6 辆车，14 个任务
  - `medium`：15 辆车，110 个任务
  - `large`：28 辆车，220 个任务
- `FleetSimulator.run()` 按离散时间推进，任务到达释放时间后才进入待调度池。

答辩说法：

> 车辆不是无限资源，每辆车都有载重、电量、速度和可用时间。仿真不是一次性把所有任务都交给策略，而是按时间步推进，任务到达释放时间后才进入 pending 队列，这样更接近真实的在线调度。

对应文件：

| 文件 | 作用 |
|---|---|
| `simulator/models.py` | 定义车辆、任务、充电站等数据类 |
| `simulator/simulation.py` | 定义三种规模和动态仿真主循环 |

---

### 要求 2：任务包含产生时间、地点坐标和货物重量，随机数生成

项目完成方式：

- `Task` 数据类包含：
  - `release_time`：任务产生时间
  - `node_id`：任务所在图节点
  - `x, y`：任务点坐标
  - `weight`：货物重量
  - `deadline`：截止时间
- `build_scenario()` 使用随机种子生成任务。
- 任务释放时间不是完全均匀随机，而是带有高峰聚集，更接近城市物流任务在某些时段集中出现的情况。
- 通过 `--seed` 保证实验可复现。

答辩说法：

> 每个任务都随机生成产生时间、任务地点和货物重量，同时设置截止时间。我们还引入了任务释放高峰，让任务不是平均分布，而是会在某些时间段集中出现，从而增加调度压力。

---

### 要求 3：任务完成越早、路径越短收益越高，超时未完成扣分

项目完成方式：

动态仿真的单任务评分在 `FleetSimulator._score_task()` 中：

```text
score = 260
        - 0.95 * response_time
        - 0.52 * distance
        - 0.25 * charging_wait
```

如果任务超时，再扣除 `overtime_penalty`。仿真结束时，未完成任务按 `unserved_penalty` 扣分。

指标包括：

- `completed`：完成任务数
- `unserved`：未完成任务数
- `overtime`：超时任务数
- `distance`：总路程
- `avg_response_time`：平均响应时间
- `charging_wait`：充电等待时间
- `score`：综合得分

答辩说法：

> 评分函数体现了题目要求：响应越快、路程越短、充电等待越少，收益越高；超时和未完成都会扣分。因此策略不能只追求完成数量，也要兼顾路径成本和时间成本。

---

### 要求 4：电量不足时寻找最近或最优充电站补能

项目完成方式：

- 车辆执行任务前，会先判断当前电量是否足够完成：
  - 从当前位置到任务点
  - 服务任务
  - 从任务点回仓
  - 保留安全电量
- 如果电量不足，调用 `_pick_charging_plan()`。
- 充电站选择不是只看最近距离，而是综合考虑：
  - 到充电站距离
  - 从充电站到任务点距离
  - 任务完成时间
  - 充电等待
  - 充电站当前负荷
- 空闲车辆还支持 `_run_idle_recharge()`，在没有任务时主动补电。

答辩说法：

> 车辆不是电量不够就直接失败，而是先判断是否能完成“去任务点再回仓”的完整路径。如果不能，就在候选充电站中选择综合代价最小的方案，代价包括行驶时间、等待时间和站点负荷。

---

### 要求 5：考虑充电站排队与负荷压力

项目完成方式：

`ChargingStation` 为每个充电端口维护 `_port_available_times`：

- 表示每个充电端口何时空闲。
- 车辆到站后选择最早可用端口。
- 实际开始充电时间：

```text
start = max(arrival_time, earliest_port_available_time)
```

- 等待时间：

```text
wait = start - arrival_time
```

- `utilization(now)` 计算当前站点负荷率。

答辩说法：

> 我们没有把充电站当成无限资源，而是为每个充电站设置端口数量。车辆到达后如果端口被占用，就要排队等待。策略在选择充电站时也会考虑站点负荷，避免所有车辆都挤到同一个站。

---

### 要求 6：可以选择是否多车协同完成同一任务

项目完成方式：

- 命令行参数 `--allow-collaboration` 控制是否开启多车协同。
- 当任务重量超过单车载重但多车总载重足够时，可以由多辆车共同完成。
- 任务重量按车辆容量比例分摊。
- `DispatchEvent` 中记录每辆车承担的重量、路线、最终电量和充电信息，前端可以回放多车协同过程。

答辩说法：

> 协同模式主要处理超重任务。单车无法完成时，系统会选择一组车辆共同服务任务，并按照车辆容量比例分摊货物重量。这样可以体现新能源物流车队的协作能力。

---

### 要求 7：至少完成两种调度策略

项目完成方式：

项目实现了 8 种动态调度策略：

| 策略名 | 思想 |
|---|---|
| `nearest_task_first` | 最近任务优先 |
| `max_task_first` | 大重量/高价值任务优先 |
| `urgency_distance` | 紧急度与距离综合权衡 |
| `auction_multi_agent` | 多智能体拍卖式派单 |
| `metaheuristic_sa` | 模拟退火搜索候选决策 |
| `lns_aaai_2025` | 大邻域搜索，破坏-修复候选派单并做局部重优化 |
| `reinforcement_q` | 简易 Q 值启发式，在线更新不同派单动作偏好 |
| `hyper_heuristic_ucb` | UCB 超启发式，在多个子规则间选择 |

答辩说法：

> 题目要求至少两种策略，我们实现了八种。前两种对应题目示例，后面几种用于提高难度分和附加分，包括多智能体、元启发式、大邻域搜索、强化学习和超启发式。

---

### 要求 8：至少模拟三种以上不同大小规模的问题

项目完成方式：

`SCENARIO_SCALES` 中定义了：

| 规模 | 节点数 | 车辆数 | 充电站数 | 任务数 | 仿真时长 |
|---|---:|---:|---:|---:|---:|
| `small` | 24 | 6 | 3 | 14 | 360 |
| `medium` | 58 | 15 | 8 | 110 | 760 |
| `large` | 100 | 28 | 12 | 220 | 1250 |
| `stress` | 42 | 8 | 2 | 55 | 430 |

答辩说法：

> 我们不是只跑一个小样例，而是设置了 small、medium、large 三档基础规模，并增加 stress 压力场景。stress 场景会降低车辆初始电量、减少充电端口并集中释放任务，用来突出充电排队和负荷压力。

---

### 要求 9.1：尝试强化学习、超启发式、元启发式、多智能体等进阶算法

项目完成方式：

项目实现了五类进阶策略：

- 多智能体：`auction_multi_agent`
  - 车辆对任务进行竞价，选择最高收益组合。
- 元启发式：`metaheuristic_sa`
  - 用模拟退火在候选任务-车辆组合中搜索较优决策。
- 大邻域搜索：`lns_aaai_2025`
  - 对候选派单解进行破坏、修复和接受判断，用于增强局部重优化能力。
- 强化学习雏形：`reinforcement_q`
  - 将系统状态离散化，用 Q 表更新动作偏好。
- 超启发式：`hyper_heuristic_ucb`
  - 使用 UCB 在最近任务、大任务、紧急度、拍卖等子策略间动态选择。

答辩说法：

> 进阶算法不是单独堆名字，而是都接入了统一的策略接口。每个策略在同一场景下运行，输出同样的指标，便于公平比较。

---

### 要求 9.2：静态全信息数学模型 + CPLEX 优化基线 + 动态对比

项目完成方式：

- `simulator/exact_solver.py` 建立静态全信息 MIP 模型。
- 假设一个时间段内所有任务已知，即“上帝视角”。
- 使用 CPLEX 求解任务分配与服务顺序；状态为最优时可作为全局最优，状态为限时结束时作为静态全信息优化基线。
- `main.py` 输出动态策略与静态全信息基线到同一个 `summary_cplex.json`。
- Web 仪表盘可以直接读取并展示对比结果。
- 当前实验环境已配置 CPLEX，`small / medium / large` 三种规模均已生成静态基线结果。

对比分析角度：

- 动态策略只能看到当前已释放任务，更符合真实实时场景。
- 静态全信息基线提前知道所有任务，可以做全局安排。
- 两者差距可以反映实时调度策略与全局优化基准之间的差距。

答辩说法：

> 我们额外实现了 CPLEX 静态全信息基线。动态策略是在线决策，只能根据当前任务池派单；CPLEX 基线假设任务全已知，用数学规划求整体调度方案。两者放在同一规模下对比，可以分析实时策略距离全局优化基准还有多少差距。

> 如果 CPLEX 输出 `time limit exceeded`，说明它在时间限制内给出了可行优化解但没有证明全局最优，因此答辩时应表述为“静态全信息限时优化基线”，这比直接说全局最优更严谨。

---

### 要求 10：图形界面展示加分

项目完成方式：

项目提供 Web 仪表盘：

- `dashboard.py` 启动本地 HTTP 服务。
- `simulator/gui.py` 提供 API。
- `simulator/web/index.html`、`styles.css`、`app.js` 实现前端。

界面功能：

- 选择规模、策略、随机种子、天气模式。
- 运行单次仿真。
- 地图/路网上回放车辆路线。
- 展示任务状态、车辆状态、充电信息。
- 展示策略排名。
- 读取 `results/summary_cplex.json` 做基准结果可视化。
- 读取 `results/weather_stats.json` 做天气/路况统计展示。
- 支持番禺本地底图和高德地图相关模式。

答辩说法：

> 图形界面不是简单展示结果表，而是能回放车辆执行任务的过程，包括车辆移动、任务完成、充电等待、策略排名和基准实验结果。这样评委可以直观看到调度过程，而不只是看 JSON。

---

## 三、系统运行流程

项目运行可以分为六步：

1. 构建场景。
   - 生成图、车辆、任务、充电站。
2. 按时间推进。
   - 每个时间步释放新任务。
3. 找空闲车辆。
   - 筛选当前可调度车辆。
4. 调用策略派单。
   - 策略返回任务和车辆组合。
5. 规划路径与补能。
   - 判断是否直达，或先去充电站。
6. 记录结果。
   - 更新车辆状态，记录任务结果和回放事件。

答辩版一句话：

> 系统是一个离散时间事件驱动仿真器，每个时间步释放任务、选择空闲车辆、调用调度策略、规划路径和充电方案，最后统计完成数、超时数、路程、等待和综合得分。

---

## 四、项目中用到的数据结构与算法

| 知识点 | 项目体现 |
|---|---|
| 图 | 城市道路网络用邻接表加权图表示 |
| Dijkstra | 车辆到任务点、充电站、仓库的最短路径 |
| 最小堆 | Dijkstra 使用 `heapq` 实现优先队列 |
| 字典 | 车辆表、充电站表、节点缓存 |
| 列表 | 待派任务池、事件序列、任务结果 |
| 排序 | 任务按释放时间排序，策略内部按优先级排序 |
| 缓存 | 最短路结果缓存，减少重复计算 |
| 离散事件仿真 | 按时间步推进任务释放和车辆状态 |
| 贪心/启发式 | 最近任务、大任务、紧急度距离综合 |
| 数学规划 | CPLEX 静态全信息基线 |

---

## 五、演示时推荐的展示顺序

### 1. 先介绍题目建模

可以说：

> 题目本质是一个带时间窗、容量、电量和充电资源约束的车辆调度问题。我们用图建模道路，用离散时间模拟任务动态出现，用多种策略完成派单。

### 2. 展示代码结构

重点展示：

- `simulator/graph.py`
- `simulator/models.py`
- `simulator/simulation.py`
- `simulator/strategies.py`
- `simulator/exact_solver.py`
- `simulator/web/`

### 3. 运行动态策略实验

命令：

```bash
python main.py --no-oracle --allow-collaboration --seed-runs 3 --output results/summary_dynamic.json
```

讲解：

> 这一步用于比较八种在线动态策略在三种规模下的表现。

### 4. 展示动态 vs CPLEX

命令：

```bash
python main.py --allow-collaboration --exact-backend cplex --exact-scales small medium large --output results/summary_cplex.json
```

讲解：

> 这一步生成动态策略和 CPLEX 静态全信息基线的同规模对比结果。

### 4.5 展示充电压力场景

命令：

```bash
python main.py --no-oracle --scales stress --strategies nearest_task_first urgency_distance lns_aaai_2025 hyper_heuristic_ucb --allow-collaboration --export-events --output results/summary_stress.json --events-output results/events_stress.json
```

讲解：

> 这一步用于突出低电量、充电站端口有限、排队等待和多车协同。结果中重点看 `charging_sessions`、`max_charging_wait`、`station_avg_utilization`、`station_peak_utilization`、`collaborative_tasks`。

### 5. 打开 Web 仪表盘

命令：

```bash
python dashboard.py
```

打开：

```text
http://127.0.0.1:8765
```

展示内容：

- 运行一次仿真。
- 播放车辆回放。
- 展示策略排名。
- 展示基准结果表和图。
- 展示天气统计。

---

## 六、老师可能问的问题和推荐回答

### Q1：你们为什么用图结构？

A：因为城市道路天然可以抽象成图。节点表示仓库、任务点、充电站或道路节点，边表示道路，边权表示距离。车辆规划路线时，本质就是在图上求最短路径。

### Q2：最短路算法是什么？复杂度是多少？

A：使用 Dijkstra 算法，并用最小堆优化。单次复杂度约为 `O((V+E)logV)`。项目还对最短路结果做缓存，减少重复路径查询。

### Q3：动态任务是怎么体现的？

A：任务有 `release_time`，仿真按时间步推进。只有当当前时间达到任务释放时间后，任务才会进入待调度池，策略才能看到它。

### Q4：车辆电量不足怎么办？

A：系统会先判断车辆是否能完成“到任务点 + 服务 + 回仓 + 保留安全电量”。如果不能，就在候选充电站中选择综合成本较低的补能方案。

### Q5：充电站排队怎么模拟？

A：每个充电站有多个端口，每个端口维护一个最早可用时间。车辆到站后选择最早空闲的端口，如果端口还没空出来，就产生等待时间。

### Q6：多车协同怎么实现？

A：如果任务重量超过单车载重，但多车容量之和足够，就可以让多辆车共同执行任务。任务重量按车辆容量比例分摊，并记录每辆车的路线和承担重量。

### Q7：为什么要有 CPLEX？

A：动态策略只能看到当前任务，是实时调度。CPLEX 静态基线假设全部任务已知，可以从全局角度求调度方案。若求解器证明最优，它就是全局最优基准；若限时结束，它仍然是有价值的静态全信息优化基线。两者对比可以看出实时策略和更强信息条件下优化方案之间的差距。

### Q8：项目难点在哪里？

A：主要难点有三个。第一，图上需要大量最短路径查询；第二，车辆有载重、电量、回仓、安全电量等多重约束；第三，充电站不是无限资源，要模拟排队和负荷压力。除此之外，还实现了多策略对比、CPLEX 基线和可视化回放。

### Q9：你们相比基础要求多做了什么？

A：基础要求只要求两种策略和三种规模。我们实现了八种策略，包括 Q 值启发式、模拟退火、大邻域搜索、超启发式和多智能体方法；还实现了 CPLEX 静态全信息基线、天气/拥堵扰动、番禺本地地图、充电压力场景和 Web 可视化回放。

---

## 七、最终答辩总结模板

可以这样总结：

> 本项目围绕新能源物流车队协同调度问题，使用图结构和 Dijkstra 最短路完成道路建模与路径规划；通过离散时间仿真模拟任务动态出现；显式建模车辆载重、电量、充电站排队和多车协同；实现了八种动态调度策略，并在 small、medium、large 三种基础规模及 stress 压力场景上进行对比；同时使用 CPLEX 建立静态全信息优化基线，分析实时调度策略和更强信息条件下优化方案的差距；最后通过 Web 仪表盘进行路线回放、策略排名和实验结果展示。整体上覆盖了大作业的基础要求，并完成了多个附加加分项。

