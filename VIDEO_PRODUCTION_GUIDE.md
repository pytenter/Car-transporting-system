# 项目展示视频制作建议

本文档用于指导新能源物流车队协同调度项目的展示视频录制。建议视频时长控制在 **6 到 8 分钟**，主线围绕“大作业要求如何被项目完整实现”展开。

---

## 1. 视频整体主线

视频不建议只录代码运行，而应按评分逻辑展示：

1. 项目解决什么问题。
2. 如何用图结构和 Dijkstra 建模道路与寻路。
3. 如何模拟新能源车辆、电量、载重、任务动态释放和充电站排队。
4. 实现了哪些调度策略。
5. 实验结果如何证明策略有效。
6. Web 仪表盘如何直观展示调度过程。

推荐开场核心表述：

> 本项目实现了一个新能源物流车队协同调度仿真系统，用图结构和 Dijkstra 完成道路寻路，用动态仿真模拟任务释放和车辆派单，并通过多策略、CPLEX 基线和 Web 可视化展示调度效果。

---

## 2. 推荐视频结构

| 时间 | 内容 | 推荐画面 |
|---:|---|---|
| 0:00 - 0:30 | 项目背景和题目要求 | README / 大作业要求 |
| 0:30 - 1:30 | 问题建模 | 项目报告 + `models.py` |
| 1:30 - 3:00 | 核心算法 | `graph.py`、`simulation.py` |
| 3:00 - 4:00 | 八种调度策略 | `strategies.py` |
| 4:00 - 5:00 | 实验结果和 CPLEX 对比 | `results/comparison_report.md` |
| 5:00 - 7:00 | Web 仪表盘演示 | Dashboard |
| 7:00 - 7:30 | 总结和加分点 | 项目报告完成情况表 |

---

## 3. 第一部分：项目背景和题目要求

建议时长：30 秒。

推荐画面：

- 打开 `README.md` 标题。
- 展示大作业要求中“新能源物流车队协同调度”的要求。

讲解重点：

- 中央仓库。
- 新能源车队。
- 城市动态任务。
- 路径规划。
- 电量和载重约束。
- 充电站排队。
- 多策略比较。

参考讲稿：

> 本项目对应的大作业题目是新能源物流车队协同调度。我们假设有一个中央仓库、一批新能源物流车和城市中动态出现的配送任务。系统需要在车辆载重、电量、充电站排队和任务截止时间约束下，决定哪辆车去完成哪个任务，并尽量提高总收益。

---

## 4. 第二部分：问题建模

建议时长：1 分钟。

推荐画面：

- `PROJECT_REPORT_TEMPLATE.md` 中的问题定义部分。
- `simulator/models.py`。
- `simulator/graph.py`。

重点展示：

| 文件 | 展示内容 |
|---|---|
| `simulator/models.py` | `Task`、`Vehicle`、`ChargingStation` |
| `simulator/graph.py` | `WeightedGraph`、`shortest_path()` |
| `simulator/simulation.py` | 场景规模、任务生成、仿真主循环 |

参考讲稿：

> 我们把城市道路建模成无向加权图。节点表示仓库、任务点、充电站或道路节点，边表示道路，边权表示距离。车辆路径规划时，通过 Dijkstra 算法计算最短路径。

> 车辆不是无限资源，每辆车有载重、电池容量、速度、能耗和可用时间。任务包含释放时间、位置、重量和截止时间。充电站有端口数和充电速率，并且会产生排队等待。

---

## 5. 第三部分：核心算法与仿真流程

建议时长：1 分 30 秒。

推荐画面：

- `simulator/graph.py` 中的 `shortest_path()`。
- `simulator/simulation.py` 中的 `FleetSimulator.run()`。
- `simulator/simulation.py` 中的 `_plan_vehicle_mission()` 和 `_pick_charging_plan()`。

### 5.1 Dijkstra 与路径成本

参考讲稿：

> 项目的底层路径成本由 Dijkstra 提供。调度策略只决定“哪个任务分给哪辆车”，而 Dijkstra 负责计算这次决策对应的路径距离、行驶时间和能耗。

### 5.2 动态任务释放

参考讲稿：

> 仿真不是一次性把所有任务交给策略，而是按时间步推进。只有当当前时间达到任务的 `release_time`，任务才会进入待派单池 `pending`。这体现了在线动态调度。

### 5.3 充电补能和排队

参考讲稿：

> 当车辆电量不足以完成任务并回仓时，系统会选择充电站补能。充电站不是无限资源，每个端口都有最早可用时间，车辆到站后如果端口被占用，就会产生排队等待。

---

## 6. 第四部分：调度策略展示

建议时长：1 分钟。

推荐画面：

- `simulator/strategies.py`。
- README 或答辩文档中的策略表。

项目实现了 8 种策略：

| 策略 | 视频中一句话说明 |
|---|---|
| `nearest_task_first` | 选择距离最近的任务-车辆组合 |
| `max_task_first` | 偏向重量大、价值高、较紧急的任务 |
| `urgency_distance` | 综合考虑紧急度、距离和车辆状态 |
| `auction_multi_agent` | 车辆对任务竞价，选择收益最高组合 |
| `metaheuristic_sa` | 用模拟退火搜索候选派单 |
| `lns_aaai_2025` | 用大邻域搜索破坏-修复候选派单 |
| `reinforcement_q` | 用 Q 表学习不同状态下的派单偏好 |
| `hyper_heuristic_ucb` | 用 UCB 在多个子策略间动态选择 |

参考讲稿：

> 大作业要求至少实现两种调度策略，我们实现了八种。前两种对应题目示例，后面几种用于提高难度分和附加分，包括多智能体、元启发式、大邻域搜索、强化学习和超启发式。

---

## 7. 第五部分：实验结果和 CPLEX 对比

建议时长：1 分钟。

推荐画面：

- `results/comparison_report.md`。
- `PROJECT_REPORT_TEMPLATE.md` 的实验结果部分。
- `results/summary_cplex.json` 可作为辅助展示。

不建议现场重新跑大型 CPLEX，直接展示已有结果即可。

讲解重点：

- 项目覆盖 `small / medium / large` 三种规模。
- 每个规模下比较八种动态策略和 CPLEX 静态全信息基线。
- CPLEX 静态全信息基线在三个规模下均取得最高综合得分。
- 不同规模下最优动态策略不同。

参考讲稿：

> 这里是已经生成的实验结果。项目覆盖 small、medium、large 三种规模。每个规模下都比较八种动态策略和 CPLEX 静态全信息基线。

> 在当前结果中，模拟退火、大邻域搜索、Q-learning 和 UCB 等进阶策略在不同规模下各有优势。CPLEX 静态全信息基线在三个规模下通常取得最高综合得分，说明全信息优化基准相对于在线动态策略有优势。

推荐措辞：

> CPLEX 静态全信息优化基线。

避免说法：

> CPLEX 已经严格证明全局最优。

---

## 8. 第六部分：Web 仪表盘演示

建议时长：1 分 30 秒到 2 分钟。

这是视频中最直观、最加分的一部分。

启动命令：

```bash
python dashboard.py
```

打开地址：

```text
http://127.0.0.1:8765
```

推荐演示顺序：

1. 展示顶部参数区。
2. 选择 `small` 场景。
3. 选择一个策略，例如 `urgency_distance` 或 `metaheuristic_sa`。
4. 点击运行仿真。
5. 播放回放。
6. 展示车辆移动、任务状态、车辆状态和日志。
7. 展示策略排名。
8. 展示基准测试结果区域。
9. 展示天气统计区域。

参考讲稿：

> Web 仪表盘用于展示调度过程。这里可以选择场景规模、策略、天气模式和随机种子。运行后，左侧地图会显示任务点、充电站、车辆路线；右侧展示车辆状态、任务状态和回放日志。

> 车辆从中央仓库出发，根据策略选择任务。若电量不足，会先去充电站补能。任务完成后车辆回仓，系统同步更新电量、位置和可用时间。

> 策略排名区域展示不同策略在同一场景下的综合得分，便于比较调度效果。下方基准测试区域读取 `results/summary_cplex.json`，可以直接比较动态策略和 CPLEX 静态基线。

---

## 9. 第七部分：总结

建议时长：30 到 45 秒。

推荐画面：

- `小白答辩技术文档.md` 的“大作业要求完成情况”。
- `PROJECT_REPORT_TEMPLATE.md` 的“完成情况”或“总结”部分。

参考讲稿：

> 总结来说，本项目完成了大作业的全部核心要求：使用图结构和 Dijkstra 实现道路寻路；模拟有限车辆、电量、载重和动态任务；考虑充电补能、充电站排队和多车协同；实现八种调度策略；覆盖三种规模；使用 CPLEX 建立静态全信息基线；并通过 Web 仪表盘进行可视化展示。

> 相比基础要求，本项目额外实现了强化学习、模拟退火、大邻域搜索、超启发式、多智能体、天气/路况扰动、地图回放和 CPLEX 对比，因此具有较完整的工程实现和实验分析。

---

## 10. 录制前检查清单

录制前建议确认：

1. Dashboard 能启动：

```bash
python dashboard.py
```

2. 以下结果文件存在：

```text
results/summary_cplex.json
results/summary_dynamic.json
results/weather_stats.json
results/comparison_report.md
```

3. 不建议现场跑大型 CPLEX。

4. 浏览器提前打开 Dashboard，避免视频里等待加载。

5. VS Code 提前打开这些文件：

```text
README.md
PROJECT_REPORT_TEMPLATE.md
小白答辩技术文档.md
simulator/models.py
simulator/graph.py
simulator/simulation.py
simulator/strategies.py
results/comparison_report.md
```

6. 演示时优先选 `small` 场景，运行快、回放清楚。

---

## 11. 视频中最应该强调的亮点

1. **图结构 + Dijkstra**  
   符合数据结构课程核心要求。

2. **动态任务释放**  
   不是静态任务列表，体现在线调度。

3. **新能源车约束**  
   电量、能耗、回仓、充电补能都建模了。

4. **充电站排队**  
   用多端口最早可用时间模拟拥塞。

5. **八种策略 + 三种规模**
   明显超过基础要求。

6. **CPLEX + Web 可视化**  
   既有进阶优化，也有直观展示。

---

## 12. 建议避免的说法

不建议说：

> 我们证明了 CPLEX 是严格全局最优。

推荐说：

> 我们用 CPLEX 建立静态全信息优化基线，在三种规模下取得最高综合得分，用于和在线动态策略对比。

不建议说：

> 车辆和任务完全随机。

推荐说：

> 车辆和任务是在合理范围内随机生成，并通过固定随机种子保证可复现。

不建议说：

> 地图模式必须联网。

推荐说：

> 系统支持随机图、本地番禺底图和高德地图模式，演示中使用已有结果和本地可视化保证稳定性。
